# 当前进度

## 已完成
2026-09-09：M0 通过，启动资料已首次提交并推送。M1 已建立 React + TypeScript + Vite 前端、Node API、独立 SQLite 最小读写及学生端/教师端/柜墙入口；AI 仅标记为 mock。
2026-09-10：M1.1 按 `design/style-2/SPEC.md` 完成“活泼卡通风”三端视觉改版。柜墙使用可复用实木柜组件、连续隔板、纸签编号及 A/B/C 五板分页；演示筛选、搜索和详情均不写业务数据。
2026-09-10：M2 完成姓名＋学号学生登录、环境隔离名单与初始积分、教师口令登录/CSV 导入/搜索/劳动加分，以及事务化、幂等的真实积分余额和流水。

## 当前阶段
M2：本地实现与验收完成，尚未提交。M3 捐赠、M4 领取、真实库存和真实 AI 均未实现；现有物品与审核区继续明确标记为界面示例或待开发。

## 当前检查点
本地分支：main。
上一已推送提交SHA：b7bb2b1dfd90b8b6b220d825dfb93544a0a0db8e（M1＋M1.1）。
工作区状态：M2 变更未提交、未推送。
测试：`npm.cmd run m2:check` 覆盖登录、同名区分、20/200 初始积分、重复 seed/导入、教师权限、奖励幂等、未授权访问、账本一致和重启持久化；`npm.cmd run typecheck` 与 `npm.cmd run build` 通过。Chrome 实测 1440×900 和 390×844：学生登录/余额/流水、教师登录/搜索/单次劳动加分均通过，截图在被忽略的 `.local-screenshots/m2/`。

## 待确定但不阻塞后续本地开发
生产服务器真实配置；柜体尺寸；AI服务商与预算；预留超时与异常退款细则。

## 每轮更新格式
保持简短：阶段 / 完成项 / 实际执行测试及结果 / 未验证项或阻塞 / 下一任务 / 上一检查点。
不要不断追加完整历史，也不要声称GitHub已推送但只完成本地commit。
