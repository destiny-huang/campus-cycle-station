# 当前进度

## 已完成
2026-09-09：M0 通过并完成启动资料首次推送；M1 建立 React + TypeScript + Vite、Node API、独立 SQLite 与三端入口。
2026-09-10：M1.1 完成第二套简约卡通风；M2 完成学生/教师登录、名单导入、初始积分与积分账本。
2026-09-10：M3 完成单件照片捐赠、A/B/C 持久化 FIFO 分柜、投放、教师审核、退回及真实柜墙；检查点 `7edfda34086369dabccaa6fe117268f6a8d799cd` 已推送至 `origin/main`。
2026-09-10：M4 已完成领取事务、幂等与并发保护、领取快照、扣分流水、柜位释放回队尾、学生领取记录及柜位异常人工处理视图；检查点 `29f4a4e6e0639301b235ddf6659280cc04c42dbe` 已推送至 `origin/main`。
2026-09-10：M4.5 完成正式登录首页、首次登录五步教程、学生随时重看及教师仅重置教程状态；检查点 `81b9d6ab191532b17bbc103b3a232ede72b91355` 已推送至 `origin/main`。
2026-09-11：M5 完成 OpenRouter 统一适配、识物与系统模板估分、审核后卡通异步队列/缓存、真实柜墙图片降级、只读工具调用助手、AI账本/日预算及安全启动脚本；Vision 增加多响应形态兼容解析。
2026-09-11：M6 完成 `2026txy` 独立生产部署，服务 `campus-cycle-station.service` 监听 9350，生产 SQLite、原图、卡通图和 EnvironmentFile 均与现有项目隔离。
2026-09-11：修复公网 HTTP 非安全上下文不支持 `crypto.randomUUID()` 导致学生端和教师端首屏空白；请求幂等 ID 增加 Web Crypto 与兼容回退。

## 当前阶段
M0–M6 已完成，生产服务运行中。

## 验证
`typecheck`、`m2:check`、`m3:check`、`m4:check`、`m45:check`、Mock `m5:check` 和生产构建均通过。Vision 使用 `qwen/qwen3.5-9b` 真实完成图片输入与结构化解析；Agent 同模型真实 tool calling 已通过；Image 使用 `google/gemini-3.1-flash-lite-image` 真实生成已通过。
服务器上同一生产构建的 M2–M5 独立临时数据检查全部通过，三类 OpenRouter 真实调用均成功；公网首页与健康接口实测 200。备份已完成 SHA-256 校验、独立临时路径恢复及 SQLite `integrity_check`。

## 数据与待定项
自动测试和浏览器验收均使用独立临时数据，日常 demo 数据未写入。幂等迁移新增AI状态、调用账本、识物缓存和卡通任务/缓存，不重建或重置既有账号、积分、流水、捐赠、领取和柜位。
原 Gemini Vision/Agent 默认模型受当前地区限制，已改用 OpenRouter 实时端点确认支持图片、结构化输出和工具调用的 `qwen/qwen3.5-9b`；AI 失败时仍降级至规则估分、原图和明确的助手暂停提示。柜体尺寸和异常退款规则仍待确定。
生产库初始为 0 学生、0 物品，不含 `DEMO001`–`DEMO006`；A/B/C 分别初始化 100/50/20 个空柜且 FIFO 正确。验收使用的临时数据已删除。

## 每轮更新格式
保持简短：阶段 / 完成项 / 实际测试及结果 / 未验证项或阻塞 / 下一任务 / 上一检查点。
