# Campus Cycle Station 工作约束

## 范围
仅维护当前校园循环站仓库。SubQuiz/newquiz及其旧仓库不是本任务工作区。
禁止读取其他工程中的密钥、修改其他工程、重启quiz、复用quiz数据库/上传目录/服务账号。
未经单独授权不得SSH到生产服务器、部署、改Nginx/防火墙/系统运行时、使用quiz的部署Runner。
不得清理、重置或覆盖既有数据库与用户上传物品；清空演示数据也必须单独授权。

## 读取与执行
先读本文件和docs/progress.md；首次或需求变更时读docs/requirements.md；再读当前阶段相关文件。
只做当前任务。不要默认全仓库扫描、重建项目、启用子Agent、接入额外MCP或安装新插件。
不读node_modules、构建产物、大型日志和历史对话。缺少必要上下文时可定向补读。
只读任务不得写文件、安装依赖、联网或提交；执行任务按当前授权允许本仓库内修改。
首次依赖安装或确需联网时走正常审批，不关闭沙箱、不全局绕过审批。
不得为本项目改全局Codex/Git配置。共享资源操作必须先停止并报告影响。

## 不改变的业务规则
赠与不是借用；学生姓名+学号登录，无激活码/短信/多因素；只设一个轻量教师端。
普通学生初始20分且只发一次；6个演示学生各200分总初始余额，不叠加普通20分。
一次一件，支持继续捐赠；投放且教师审核后才加分并上架。
AI建议积分，教师可改；ABC分别FIFO自动分配；领取成功立即扣分、核销、柜位入队尾。
不增加取走确认或教师逐单批准领取，不添加未请求的商城支付、借还、排行榜或多级教师权限。
简化身份验证不等于允许前端直接决定积分；所有库存和积分变更必须由后端校验。

## 质量与同步
重复点击/回调不能重复发扣积分；并发领取/分柜不能重复成功；失败必须保持原状态。
先用模拟AI跑通，明确标记模拟结果；真实AI调用须单独配置、限并发、缓存、可重试且不发重积分。
真实名单、密钥、数据库和用户上传不入Git，即使私有仓库也一样。
每轮运行相关测试；没有运行的测试标明未验证，不能用静态检查冒充端到端通过。
提交或push仅在用户授权时执行；不force push、不删除远端、不使用git reset --hard。
完成后原地更新docs/progress.md，输出改动/测试/阻塞/提交与推送状态；不要重新粘贴整份需求。
进度可记录上一检查点SHA；不要试图让提交内文件包含该提交自身SHA。
