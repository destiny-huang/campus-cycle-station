# 后续任务模板

执行M【阶段号】，本轮目标：【一个明确功能】。
先读AGENTS.md、docs/progress.md、roadmap对应阶段，再定向读取相关代码，不扫描无关目录。
保持需求基线，不重建、不增加未请求功能、不调用其他Agent、不访问quiz或服务器。
验收：【2—4条可测试结果】。
实际执行相关测试，未执行的标记；只更新短进度，暂不commit/push，完成后报告改动/测试/阻塞。

# 体验通过后：保存检查点

本轮功能已体验通过。检查当前分支、工作区、远端和即将提交的文件，不覆盖我未提交的其他改动。
运行相关检查并检查敏感数据遗漏；只提交本轮已审阅文件到campus-cycle-station，获正常联网许可后push到对应分支。
不force push，不访问其他仓库或生产服务器。返回commit SHA、push是否成功、实际测试摘要。
若远端先行或冲突，停止并报告，不自动覆盖。
