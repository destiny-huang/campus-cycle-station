执行 M1.1：第二套“活泼卡通风”的简约版UI改造。用户已经选定风格，不再做方案比较。

工作目录：E:\Work-2\campus-cycle-station
唯一仓库：destiny-huang/campus-cycle-station

先遵守 AGENTS.md，读取 docs/progress.md、本目录SPEC.md和references中的三张图片。
用可用的本地图片查看能力真正看图；读不到时报告缺失/限制，不能只看文件名就声称参考过。
读取顺序：SPEC约定优先于图中装饰、错误业务数字和占位文案。

授权范围：当前工程内与三端UI相关的组件、样式、只读演示fixture和必要小素材；运行已有检查与本地服务。
本轮不做M2，不实现登录/积分/捐赠/领取业务，不改后端或SQLite，不调用付费AI。

执行方式：
1. 简短核对目录、origin与本地M1源码。M1可能尚未提交，保留全部现有改动，禁止git restore/clean/reset。
   远端只有启动文档也不能用远端覆盖本地M1；本地缺少M1源码则停止报告，不新建第二个工程。
   把将修改的少量前端源文件备份到项目内已Git忽略的.local-backups/style2-before-时间戳/，不复制数据库、凭据、依赖或整仓。
2. 本轮完整改学生端、教师端、柜墙的统一视觉，重点先做可复用木柜/隔板/格位组件，再统一其他两端。
   不仅改背景颜色：清除大圆角套卡片、缩减宣传区、木柜必须有厚外框和连续横竖隔板。
3. 只用文字规范和本地素材完成已有工程中的可编辑页面，不把参考图整张铺上去，不接外部图片热链。
   优先已有依赖与原生HTML/CSS/SVG；不升级Node/全局环境，不安装大框架/3D库/浏览器插件。
4. 保留所有原路由、启动脚本和健康检查。只加演示层导航/筛选/分页/示例详情/轻跳动画，数据明确标为示例。
   真实业务未实现的操作要如实提示待开发，不假加分/假上架/假领取；不要用localStorage假冒业务数据库。
5. 实际运行typecheck、build、本地服务和健康接口；能用现有浏览器工具则按SPEC做桌面/手机视觉检查及截图。
   不能视觉检查时如实报告，不能仅靠HTTP200判定像参考图。每类问题集中修正一次，避免无止境重画。
6. 更新简短进度为“M1.1视觉改版待用户验收，M2未开始”，并引用design/style-2/SPEC.md供后续沿用。

不访问SubQuiz/其他工程，不连接腾讯云，不部署；不启用子Agent/MCP/新插件，不全仓扫描，不联网拉新素材。
本轮不git add/commit/push；仅可增补本项目.gitignore以排除源码备份、测试截图临时目录，不隐藏正式源码。
若现有能力不足以完成关键视觉，指出具体缺少的素材/工具，不伪称像素级复刻。

最终只报告：改动、实际检查结果、启动命令与地址、服务是否运行、未完成项、可用的真实网页截图路径。
明确UI改版不等于M2业务已实现。完成后停止，等待用户看页面。
